<section class="page-hero">
  <div class="container">
    <p class="eyebrow">{{ i18n[lang].nav.about }}</p>
    <h1 class="display">{{ i18n[lang].about.title }}</h1>
    <p class="lead">{{ i18n[lang].about.subtitle }}</p>
  </div>
</section>

<section class="container section about__body">
  <div class="prose prose--editorial">
    <p>{{ i18n[lang].about.p1 }}</p>
    <p>{{ i18n[lang].about.p2 }}</p>
    <p>{{ i18n[lang].about.p3 }}</p>
  </div>
</section>

<section class="container section">
  <header class="section-header">
    <p class="eyebrow">{{ i18n[lang].about.where_title }}</p>
    <h2 class="display">{{ i18n[lang].about.where_title }}</h2>
  </header>
  <p class="lead">{{ i18n[lang].about.where_text }}</p>
</section>
